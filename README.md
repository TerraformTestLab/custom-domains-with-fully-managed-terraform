# custom-domains-with-fully-managed-terraform

Terraform configuration for enabling HCP Vault custom domains using the fully managed approach.
Currently provides an AWS setup under `aws/` that provisions the required DNS, ACM, and networking resources.
See `aws/README.md` and `aws/docs/` for prerequisites, usage, and testing details.
