# custom-domains-with-fully-managed-terraform

Terraform configuration for enabling HCP Vault custom domains using the fully managed approach.
Currently, provides an AWS setup under [aws](aws) that provisions the required DNS, ACM, and networking resources to enable custom domains using AWS only resources.
See [aws/README.md](aws/README.md) to know more
